소수 찾기 성공
시간 제한	메모리 제한	제출	정답	맞힌 사람	정답 비율
2 초	128 MB	215284	102026	81681	47.239%
문제
주어진 수 N개 중에서 소수가 몇 개인지 찾아서 출력하는 프로그램을 작성하시오.

입력
첫 줄에 수의 개수 N이 주어진다. N은 100이하이다. 다음으로 N개의 수가 주어지는데 수는 1,000 이하의 자연수이다.

출력
주어진 수들 중 소수의 개수를 출력한다.

예제 입력 1 
4
1 3 5 7
예제 출력 1 
3


solution

#include<stdio.h>
#include<math.h>

#define SIZE 1001

int main() {
	int n;
	int num[100];
	int count = 0;

	int a[SIZE] = { 0 };	// 0 ~ 1000
	int i, j;

	//에라토스테네스의채///////
	for (i = 2; i <= sqrt(SIZE); i++) {	// 최대값의 제곱근까지 반복
		if (a[i] == 0) {		//i가 소수이면
			for (j = 2; i * j < SIZE; j++) {	// 자신을 제외한 i의 배수 제거
				a[i * j] = 1;
			}
		}
	}
	a[0] = 1;  // 0은 소수 아님
	a[1] = 1;  // 1은 소수 아님

	scanf("%d", &n);    //수의 개수
	for (int i = 0; i < n; i++) {
		scanf("%d", &num[i]);       //n개의 수 입력
	}

	for (int i = 0; i < n; i++) {
		if (a[num[i]] == 0)
			count++;                    // 입력한 수가 소수면 count++
	}

	//소수 개수 출력///////
	printf("%d", count);

	return 0;
}
